---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Sidebar from '../components/Sidebar.astro';
import home from '../features/home/img/home.png';
import Menu from '../components/Menu.astro';

const menuList = ['/ai', '/article', '/learning', '/useful', '/material'];
const targetMenu = menuList[Math.floor(Math.random() * menuList.length)];
---

<Layout>
    <div class="h-screen relative">
        <Menu />
        <Header />
        <div class="flex h-[calc(100%_-_3rem)]">
            <Sidebar />
            <main class="sm:w-5/6 flex px-6 bg-white">
                <div class="w-full sm:w-1/2 pt-6 relative">
                    <h3 class="text-xl mb-2 font-bold">
                        もっと深く、<br class="sm:hidden" />もっと広く開発を楽しむ
                    </h3>
                    <p>
                        このサイトでは管理人が開発を楽しくするために、開発に関係する学習サイト・便利サイトなどをまとめています。新しいサイトを見つけるたびに更新します。<br
                        />
                        どこかの誰かの役に立ちますように。
                    </p>
                    <a href={targetMenu}>
                        <img class="block w-4/5" src={home} alt="学習をしている画像" />
                    </a>
                    <a
                        class="text-xs text-blue-600"
                        href="https://storyset.com/education"
                        target="_blank"
                        rel="noreferrer">Education illustrations by Storyset</a
                    >
                </div>
            </main>
        </div>
    </div>
</Layout>
